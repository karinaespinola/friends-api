{
  "name": "friends-api",
  "version": "1.0.0",
  "description": "API to get some info from the characters of the Friends series",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm start nodemon server.js"
  },
  "author": "Karina Espinola",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "mongoose": "^6.1.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
